/**
 * Max width for primary/secondary actions in stacked forms.
 * Keeps CTAs from stretching edge-to-edge on large screens and matches a comfortable tap target width on phones.
 */
export const FORM_PRIMARY_ACTION_MAX_WIDTH = 360;
