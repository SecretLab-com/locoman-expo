# Active Context

## Current Focus
- **Shared UI adoption rollout**: Push the newer screen-shell, row/menu, chip/filter, and modal-header abstractions into real trainer/admin/client screens so duplication drops in practice rather than only in theory
- **Trainer attribution and commissions**: Dedicated attribution model, store-link flow, independent shopping commission pipeline, and attributed earnings visibility
- **Design system centralization**: Semantic tokens, shared primitives, and lint enforcement for raw UI literals
- **Calendar UI redesign**: Google Calendar-style layout with fixed compact grid at top, scrollable sessions below
- **Reschedule requests**: Full workflow with approve/reject/counter-propose UI on calendar alerts
- **AI Assistant**: OpenRouter LLM (Gemini 2.5 Flash) with tool-calling, vision, and voice input
- **Google Calendar integration**: Two-way sync, auto-create "Locomotivate" calendar, event deletion on cancellation
- **Accessibility audit**: Adding `accessibilityRole`, `accessibilityLabel`, and `testID` to interactive components
- **Campaign post attribution**: Rule-based social post matching and attribution-backed campaign compliance
- **Trainer home social-state UX**: Cached social card state with skeleton fallback to prevent invite-state flicker for signed-up trainers
- **Social membership lifecycle hardening**: Invite/pause/ban flows now need to keep `trainer_social_memberships`, `trainer_social_invites`, trainer-facing visibility, and admin audit logs in sync

## Recent Changes
- **Component adoption pass**: Expanded real usage of the shared UI layer across high-duplication screens. `app/(trainer|manager|coordinator)/more.tsx` now share `MoreMenuScreen`; repeated auth/loading/empty shells in screens like `app/(tabs)/messages.tsx`, `app/(manager)/analytics.tsx`, and `app/(trainer)/rewards.tsx` use `SignedOutGate`, `AsyncScreenBoundary`, `LoadingState`, and `EmptyState`; `app/(tabs)/profile.tsx` now routes its menu rows through `ListRow`; `app/(coordinator)/template-settings.tsx` uses `Chip` for repeated selection pills; and modal-heavy flows like `app/(tabs)/cart.tsx` and `app/(trainer)/calendar.tsx` now use `ModalHeader`.
- **Plan shopping fly-to-cart animations**: Added a global `CartAnimationProvider` + `CartFlyOverlay` driven by `react-native-reanimated`. `PlanShoppingShell` now registers the footer summary as a cart target (plus a cached fallback rect), `addItem` accepts optional animation metadata, product add buttons pass source refs, and plan-shopping bundle detail delays `router.back()` until the bundle chip flight completes. Reduced-motion falls back to a footer pulse instead of a full fly animation.
- **Plan-shopping bundle route shell fix**: Bundle taps from trainer plan shopping now open the root `app/bundle/[id].tsx` route instead of the trainer-scoped tab route, so the trainer bottom nav does not appear. In plan-shopping mode, bundle detail renders inside `PlanShoppingShell` with `hideHeader`, keeping its own back/share chrome while showing the same cart summary footer as Bundles/Categories/Products.
- **Plan-shopping Custom tab**: `app/(tabs)/products.tsx` now exposes a trainer-only `Custom` tab during plan shopping, powered by `trpc.customProducts.list/create/update/delete`. The old custom-product picker/create modal was removed from `app/(tabs)/cart.tsx`, so custom products are chosen and managed in the shopping step alongside Bundles/Categories/Products, while the review screen keeps only the Add Sessions shortcut. Custom product images now use `SingleImagePicker` and upload through `trpc.messages.uploadAttachment` on save instead of asking trainers for a raw image URL.
- **Proposal deep link → cart hydration**: Opening the trainer cart with `?proposalId=<uuid>` (e.g. from `getTrainerProposalDeepLink`) loads `savedCartProposals.get`, replaces cart + `proposalContext` from the snapshot, strips the query via `router.replace(pathname)`, and shows a short loading overlay + error alert on failure (`app/(tabs)/cart.tsx` `TrainerProposalBuilder`).
- **Loco Assistant — Custom Plan parity**: The in-app assistant `create_saved_cart_proposal` tool now aligns with `savedCartProposals.create` (`programWeeks`, `sessionsPerWeek`, `sessionDurationMinutes`, `sessionCost`, `assistantPrompt`, merged plan metadata like tRPC). `list_bundles` returns `sessionCount` + `estimatedProgramWeeksHint`; new `get_bundle` returns the same plus raw JSON. Successful creates include `proposalUrl` from `getTrainerProposalDeepLink` (`EXPO_PUBLIC_APP_URL` + `/cart?proposalId=`). MCP: enriched `list_bundles`, new `get_bundle` and `get_saved_cart_proposal`, `create_saved_cart_proposal` includes plan fields + `proposalUrl` on success. Holiday/blackout skipping remains **notes-only** (no schedule engine change).
- **Bundle / Custom Plan naming**: Trainer and client UI copy now uses **Bundle/Bundles** for published trainer items and **Custom plan** for tailored saved-cart flows; internal helpers were renamed (`mapBundleDraftToBundleView`, `getBundleFallbackImageUrl`) and launch analytics events updated (`trainer_add_client_bundle_deferred`, `trainer_add_client_custom_plan_start`, `trainer_bundle_*`) without changing DB tables, `trpc.bundles` namespaces, or route paths like `/(trainer)/offers`.
- **Plan-shop Step 1 intro**: Closing the intro (`X` or Android back) exits immediately via `exitPlanDiscard` (clear cart + back)—no second confirm sheet. After **Start Shopping**, shell/review still use `PlanFlowCancelModal` when leaving with possible cart contents.
- **Trainer plan review MVP**: Removed the **Save Plan** button from Review & Send Plan (`app/(tabs)/cart.tsx`). There is no in-app list for drafts yet; **Send Plan** still persists via `persistProposal()` + `sendInvite`. Backend `savedCartProposals` create/update remains for invite flow and future drafts UI.
- **iOS plan schedule pickers**: Review & Send plan modals no longer use `@react-native-community/datetimepicker` on iOS (avoids red `Unimplemented component: <RNDateTimePicker>` when the Fabric view is missing from the dev-client binary). `PlanTimePreferenceField` uses JS scroll wheels (`IosTimeWheelPicker`); `PlanStartDateField` uses a snapped day list (`IosDateListPicker`). Android still uses the native picker.
- **Plan-shop offers navigation + scope**: Tapping an offer in the trainer plan builder routes to `/(trainer)/bundle/[id]` (same as the trainer Offers list). The Offers list uses `trpc.bundles.list` (loading gated on that query) and only **published** rows so the plan builder matches `/(trainer)/bundles` and does not flash empty while `catalog.bundles` was still loading.
- **Trainer attribution model**: Added dedicated `trainer_attributions` and `trainer_attribution_log` tables (migration 024), with `attribution_source` enum, per-product/bundle `commission_rate` columns, and `attribution_id` on orders for traceability
- **My Store Link**: Trainers now see a "My Store Link" card on their profile with native share; the link routes to `app/shop/[slug].tsx` which resolves the trainer by username, sets attribution (or saves a pending onboarding context before auth), and routes shoppers into the existing storefront
- **Attribution on invite/order**: Both `acceptInvitation` and `createFromProposal` now upsert the attribution model with `invitation_acceptance` source; `orders.create` resolves the customer's current attributed trainer when the cart has no explicit trainer; Shopify order payloads include `attributed_trainer_id` and `attribution_id` in `note_attributes`
- **Commission pipeline**: Paid orders now create `trainer_earnings` commission rows via `createAttributedCommissions` using per-product `commission_rate` or a default 10%; the trainer earnings screen now distinguishes commission, bonus, and sale types with distinct icons
- **Attributed purchase notification**: `notifyTrainerAboutAttributedPurchase` sends a push notification to the attributed trainer when a customer's independent purchase is paid
- **Delivery journey doc audit**: The implementation checklist in `docs/BUNDLE_TO_DELIVERY_JOURNEY_UPDATE.md` was corrected to reflect the real codebase state, with stale unchecked items flipped and partially-built items split into granular sub-tasks
- **Design system foundation**: Expanded `theme.config.js` into a broader token source of truth for semantic colors, spacing, radii, typography, and elevation; exposed those through `lib/_core/theme.ts`, `tailwind.config.js`, and `lib/theme-provider.tsx`
- **Shared UI primitives**: Added reusable design-system helpers and primitives for typography, cards, badges, icon buttons, inputs, FABs, modal surfaces, and divider recipes, then moved shared FAB/menu surfaces plus major trainer/admin screens toward them
- **Raw-style lint gate**: Added a custom ESLint rule plus a temporary exception list so new raw hex/rgba/font/shadow literals are blocked outside the approved migration surface
- **Exception-list burn-down**: Removed already-clean files like `components/profile-fab.tsx`, `components/impersonation-banner.tsx`, `components/adyen-checkout.tsx`, and `app/(trainer)/templates.tsx` from the design-system exception list while trimming warnings in the touched trainer/shared files
- **Second burn-down pass**: Removed additional low-risk files such as `app/(client)/orders.tsx`, `app/oauth/callback.tsx`, `app/share-intent.tsx`, `app/(coordinator)/index.tsx`, `app/(trainer)/payment-history.tsx`, `app/(manager)/deliveries.tsx`, `app/campaign/[slug].tsx`, and `components/service-picker-modal.tsx` from the design-system exception list while keeping `pnpm lint:design-system` error-free
- **Phyllo dual-path sync**: Social data now syncs through both webhook ingestion and direct API pulls; manual/connect syncs backfill content rows, and the server periodically polls connected Phyllo profiles into Supabase so missed webhooks no longer leave the UI empty
- **Exclusive trainer social ownership**: Phyllo connect/sync now rejects duplicate social identities across trainers by checking `phyllo_user_id`, account ids, and platform-specific identifiers like `platform_profile_id`, usernames, and profile URLs before persisting profile updates
- **Conflict audit surface**: Duplicate social-account blocks now create manager/coordinator `social_event_notifications` entries and appear in the social management dashboard as an account-conflict audit card
- **Phyllo recent-post autofill**: The `socialProgram.recentPosts` query now self-heals empty trainer content by triggering a direct Phyllo pull when a connected `phylloUserId` exists, so the UI can populate posts even before a webhook or scheduled poll has written them
- **Phyllo manual full sync**: The visible `Sync now` action on `social-progress` now runs the full manual Phyllo sync path, including profile refresh, direct content pull, campaign post attribution refresh, and campaign metrics invalidation in the UI
- **Phyllo production ops**: `DEPLOYMENT_GUIDE.md` now includes the exact Cloud Run env vars and Cloud Scheduler create/update commands for the deployed backend (`locoman-486301`, `us-central1`, `locoman-backend`) using the authenticated periodic-sync endpoint
- **Phyllo V/MO fallback**: Trainer social cards now derive `V/MO` from imported recent-post views when profile-level monthly views are missing, and the pull sync persists that fallback into `avgViewsPerMonth` so YouTube view data no longer displays as zero
- **Connected services impressions fallback**: The `social-progress` connected-services list now merges imported recent-post view totals into per-platform impressions and uses subscriber-count fallbacks for YouTube followers, so the YouTube service row no longer shows `0` when synced post metrics exist
- **Manual payout KYC workflow**: Replaced the trainer Adyen link-out flow with a manual-first Bright.Blue intake + status tracker. Trainers now submit minimal account-holder details in-app, coordinators/managers manage status in a dedicated KYC queue, and social management shows payout KYC summary/status visibility.
- **Adyen-aligned KYC statuses**: Bright.Blue payout onboarding now tracks the user-facing states `Start setup`, `Details submitted`, `Verification required`, `Under review`, `More information required`, `Active`, `Verification failed`, and `Account rejected`, with the KYC doc updated to map these to Adyen's lower-level verification outcomes.
- **Social management member UX**: The admin social dashboard now shows a compact top-10 member list ranked by subscribers, opens member actions from a detail modal on row tap, and moves search/sort/filter into a dedicated `Manage` member list modal instead of showing inline action buttons in the overview.
- **Trainer offer flow consolidation**: Trainer create/edit offer entry points are being retired off the duplicate `app/(trainer)/offers/new.tsx` wizard and moved onto the original `app/bundle-editor/[id].tsx` flow, with trainer-facing bundle reads replacing the old `trpc.offers.*` path.
- **Trainer saved-cart shopping fix**: Trainer bundle shopping now keeps the shopper-style add-to-cart CTA on the root bundle detail route, role-specific bundle lists route back to their scoped bundle detail screens, and the trainer saved-cart builder now returns to `/(trainer)/products` instead of the generic tab products route.
- **Trainer plan UX cleanup**: Trainer-facing copy now presents one plan journey: `Create Plan` starts catalog browsing, `Review & Send` opens the trainer cart review screen, and trainer client list/detail surfaces expose a `Create Plan` CTA that preselects the client before shopping.
- **Focused plan-shopping flow**: Trainer plan shopping now opens as a dedicated fullscreen route (`/plan-shop`) outside the trainer tab layout, with a Step 1 intro modal, close button, floating client identity chip, plan-status footer with Done button, and no bottom nav. Entry points from the clients list and client detail preselect the client and open the focused flow, while the More shortcut now routes trainers back to the clients list first instead of showing a duplicate picker.
- **Product detail modal scroll fix**: The catalog product bottom sheet no longer steals responder events from inner scroll content, and the sheet now has a bounded max height so long descriptions can scroll to the CTA on native.
- **Social membership lifecycle**: Social management now shows inline success/error feedback for invite/pause/ban actions, realtime invalidation listens to `trainer_social_memberships` and `trainer_social_invites`, banning a trainer revokes pending invites and resets membership to `uninvited`, and `socialProgram.myStatus` now hides profile/commitment/progress details whenever a trainer is not currently invited into the program
- **Counter-propose UI**: Reschedule request alerts now include a "Suggest" button that expands an inline form for date/time/note counter-proposals
- **Client spending categorization**: Orders now categorized as sessions/subscriptions/products based on description and fulfillment method (was previously all "products")
- **Accessibility labels**: Added `accessibilityRole="button"`, `accessibilityLabel`, and `testID` to trainer MoreRow components
- **Calendar layout**: Redesigned to Google Calendar mobile style (fixed compact grid, scrollable session list)
- **Schedule form**: Light mode support, field chaining (Next/Done buttons), auto-scroll to focused inputs, inline validation
- **AI Assistant (Loco Assistant)**: Full-featured chat with OpenRouter LLM, tool-calling for business data, vision support for images
- **Voice input**: Mic button in chat input, records audio, transcribes via Groq Whisper, auto-sends for assistant chats
- **OpenRouter LLM**: Replaced Forge API with OpenRouter for cost-effectiveness and multi-provider flexibility
- **Google Calendar sync**: Two-way sync creates reschedule requests for moved events, cancels sessions for deleted events
- **Auto-create Locomotivate calendar**: `googleCalendar.connectWithCode` creates dedicated calendar if none exists
- **Modal backgrounds**: Fixed transparency/overlap issues across 16 files using inline styles instead of NativeWind className
- **Sponsored products**: Database schema for trainer bonus, sponsored_by, bonus_expires_at, is_sponsored fields
- **MCP server**: HTTP routes with auto-refresh for expired JWT tokens, graceful skip in production builds
- **OpenClaw MCP**: Connection endpoint, token auto-refresh, role-based visibility in settings
- **Bundle creation from templates**: Copies template product/service/goal data, pre-populates form fields
- **Campaign post attribution**: Added posting rules on campaign account metadata, durable post-to-campaign attribution storage, webhook-time matching, attribution-backed campaign metrics, and trainer/coordinator compliance UI
- **Trainer home social card**: Added local `AsyncStorage` cache for `socialProgram.myStatus`, trusted stale active/connected states, and skeleton fallback when no trustworthy social status is available yet
- **WebSocket offline noise**: Client websocket hook now pauses reconnect attempts and suppresses expected offline disconnect spam until the network is restored

## Next Steps / Remaining TODOs
- **Phyllo ops hardening**: Consider admin visibility/replay tooling for failed or delayed Phyllo webhook/poll sync runs
- **Campaign attribution backfill ops**: Consider adding an explicit admin backfill/replay action for historical posts if campaign rules change at scale
- **Google Calendar webhook**: Real-time push notifications from Google (currently polling-based sync)
- **Partnerships**: Still using mock data, needs real API
- **Checkout flow**: Needs completion and payment processing integration (Adyen Drop-in requires native module setup)
- **Revenue analytics**: Some mock data remains, needs real calculations
- **Testing**: Full end-to-end testing of all flows

## Decisions
- **OpenRouter over Forge**: Chosen for cost control and multi-provider flexibility (Gemini 2.5 Flash default)
- **Groq Whisper**: Used for speech-to-text (whisper-large-v3 model)
- **Reschedule requests over auto-update**: Google Calendar moves create approval requests instead of auto-changing sessions
- **Supabase as single source**: Database + Auth provider
- **Service role key**: Used for all server-side DB operations (bypasses RLS)
- **Snake_case in DB**: Postgres uses snake_case, app code uses camelCase with conversion layer
- **Inline styles for modals on web**: NativeWind className unreliable inside React Native Modal on web
